package com.whyjoin.branch;

/**
 * Enumerator class that represents all of the branches of the United States
 * military. Assigned to a a user and post as a tag, so all values that match
 * are queried together.
 */
public enum Branch {
    USMC,
    USN,
    USA,
    USAF,
    NG,
    CG
}
